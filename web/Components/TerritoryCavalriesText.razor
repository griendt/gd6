@using web.Models
<circle
    r="@(14 + 4 * (Territory.Cavalries / 10))"
    cx="@(Territory.Centroid.X + 5)"
    cy="@(Territory.Centroid.Y - 30)"
    fill="#a60c"
    stroke="#830"
    stroke-width="2px"/>

<text
    x="@(Territory.Centroid.X + 5)"
    y="@(Territory.Centroid.Y - 30)"
    text-anchor="middle"
    font-family="Noticia"
    font-size="24"
    dominant-baseline="central"
    fill="@Fill"
    stroke="@Stroke"
    stroke-width="@StrokeWidth"
    style="user-select: none; pointer-events: none">
    @Territory.Cavalries
</text>

@code {
    [Parameter] public required Territory Territory { get; set; }
    [Parameter] public string Fill { get; set; } = "var(--bs-light)";
    [Parameter] public string Stroke { get; set; } = "#000";
    [Parameter] public string StrokeWidth { get; set; } = "0";
}
